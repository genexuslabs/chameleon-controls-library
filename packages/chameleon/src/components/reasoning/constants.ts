/**
 * Default message displayed in the accordion trigger while reasoning is streaming
 */
export const DEFAULT_THINKING_MESSAGE = "Thinking...";

/**
 * Default template message displayed in the accordion trigger after reasoning completes.
 * Use {duration} placeholder to insert the duration value.
 */
export const DEFAULT_THOUGHT_MESSAGE_TEMPLATE = "Thought for {duration} seconds";

/**
 * Placeholder token used in thought message templates to be replaced with actual duration
 */
export const DURATION_PLACEHOLDER = "{duration}";

/**
 * Default streaming speed in milliseconds per character for the typewriter effect
 */
export const DEFAULT_STREAMING_SPEED_MS = 20;
