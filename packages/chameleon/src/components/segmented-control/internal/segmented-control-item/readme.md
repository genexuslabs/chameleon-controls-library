# `ch-segmented-control-item`

<p>Segmented control is used to pick one choice from a linear set of closely related choices, and immediately apply that selection.
This control represents and item of the ch-segmented-control-render</p>

<details open>
  <summary>
  
  ## Properties
  </summary>
  
### `accessibleName:  string | undefined`

<p>Specifies a short string, typically 1 to 3 words, that authors associate
with an element to provide users of assistive technologies with a label
for the element.</p>

**Attribute**: <code>accessiblename</code>

**Default**: <code>undefined</code>

---

### `between:  boolean`

<p><code>true</code> if the control is the not the first or last item in the
ch-segmented-control-render.</p>

**Attribute**: <code>between</code>

**Default**: <code>false</code>

---

### `caption:  string | undefined`

<p>Specifies the caption that the control will display.</p>

**Attribute**: <code>caption</code>

**Default**: <code>undefined</code>

---

### `disabled:  boolean | undefined`

<p>This attribute lets you specify if the element is disabled.
If disabled, it will not fire any user interaction related event
(for example, click event).</p>

**Attribute**: <code>disabled</code>

**Default**: <code>undefined</code>

---

### `endImgSrc:  string | undefined`

<p>Specifies the src of the end image.</p>

**Attribute**: <code>endimgsrc</code>

**Default**: <code>undefined</code>

---

### `endImgType:  Exclude<ImageRender, "img"> | undefined`

<p>Specifies how the end image will be rendered.</p>

**Attribute**: <code>endimgtype</code>

**Default**: <code>undefined</code>

---

### `first:  boolean`

<p><code>true</code> if the control is the first item in the ch-segmented-control-render.</p>

**Attribute**: <code>first</code>

**Default**: <code>false</code>

---

### `last:  boolean`

<p><code>true</code> if the control is the last item in the ch-segmented-control-render.</p>

**Attribute**: <code>last</code>

**Default**: <code>false</code>

---

### `selected:  boolean | undefined`

<p>Specifies if the control is selected.</p>

**Attribute**: <code>selected</code>

**Default**: <code>undefined</code>

---

### `startImgSrc:  string | undefined`

<p>Specifies the src of the start image.</p>

**Attribute**: <code>startimgsrc</code>

**Default**: <code>undefined</code>

---

### `startImgType:  Exclude<ImageRender, "img"> | undefined`

<p>Specifies how the start image will be rendered.</p>

**Attribute**: <code>startimgtype</code>

**Default**: <code>undefined</code>
</details>

<details open>
  <summary>
  
  ## CSS Parts
  </summary>
  
### `selected`

<p>...</p>
</details>
