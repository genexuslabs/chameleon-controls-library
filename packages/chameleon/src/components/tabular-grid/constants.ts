/**
 * Optimize render performance: Internal components don't need sync properties
 * with attributes.
 */
export const TABULAR_GRID_NO_ATTRIBUTE = { attribute: false };

export const TABULAR_GRID_BOOLEAN_TYPE_PROP = { type: Boolean };

