

init() {
    setHideableCols(); OK
    this.hideCols(); OK
}

Cuando oculto/muestro una columna se emite el evento toggledColumn
@Listen("toggledColumn")
toggledColumnHandler {
    agrega o quita el attributo "hidden" a la columna toggleada
    setHideableCols(); OK
    hideCols(); OK
}

cuando muevo una columna se emite el evento moveCol
  @Listen("moveCol")
  moveColHandler(e) {
    Se swapea la columna origen por la destino, cambiando sus gridColum y data-index
    setGridTemplateColumns();
  }

hideCols() {
    let hiddenCols = [];

    hiddenCols contiene un array con los indices de las columnas ocultas.
    Luego se recorren las columnas y las celdas, y se ocultan o se muestran
    segun si hiddenCols[i] es true o false;

    setGridTemplateColumns(); OK
    setGridColumnsOrder(); 
    setGridCellsOrder();    
}

setGridTemplateColumns(){
    OK!
    Define el gridTemplateColumns teniendo en cuenta solo las columnas visibles, ordenadas
    por index.
}
setGridColumnsOrder(){
    Aca esta el tema!
}

setGridCellsOrder(){

}
setHideableCols(){
    obtiene las columnas hiddeables y su valor hidden/visible y emite esta
    informacion para ser usada en ch-grid-menu.
}